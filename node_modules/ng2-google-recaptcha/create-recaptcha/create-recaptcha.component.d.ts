import { OnInit, EventEmitter } from '@angular/core';
export declare class CreateRecaptchaComponent implements OnInit {
    onCaptchaComplete: EventEmitter<any>;
    siteKey: string;
    captchaStyle: any;
    renderDelay: number;
    recaptchaId: string;
    private recaptchaInstance;
    ngOnInit(): void;
    resetRecaptcha(): void;
    private onCaptchaCompleted(data);
}
